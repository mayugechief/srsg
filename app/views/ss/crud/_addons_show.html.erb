<% if @model && (addons = @model.addons).size > 0 %>

<% addons.each do |addon| %>
<div class="addon-view" id="addon-<%= addon.object_id %>">
  <%= render_cell "#{addon.path}/edit", params[:action] %>
</div>
<% end %>

<% end %>
