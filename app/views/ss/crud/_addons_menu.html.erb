<% if @model && (addons = @model.addons).size > 0 %>
<nav class="addon-menu">
  <header><h1><%= lang :addons %></h1></header>
  <div>
    <%= link_to :all, "#", class: "addon-tab", "data-id" => "#addon-all" %>
    <%= link_to :basic_info, "#basic", class: "addon-tab", "data-id" => "#addon-basic" %>
    
    <% addons.each do |addon| %>
      <%= link_to addon.name, "##{addon.object_id}", class: "addon-tab",
        "data-id" => "#addon-#{addon.object_id}" %>
    <% end %>
  </div>
</nav>
<% end %>
