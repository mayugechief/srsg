<table class="index">
  <thead>
    <tr>
      <th style="width: 20px;"><input type="checkbox" /></th>
      <th><%= @model.lang :name %></th>
      <th><%= @model.lang :filename %></th>
      <th><%= @model.lang :updated %></th>
    </tr>
  </thead>
  
  <tbody>
    <% @items.each do |item| %>
    <tr>
      <td>
        <input type="checkbox" name="ids[]" value="<%= item.id %>" />
        <nav class="row-menu">
          <%= link_to "コンテンツ管理", "/.#{@cur_site.host}/#{item.route.sub(/\/.*/, '')}#{item.id}" if item.route.present? %>
          <%= link_to "詳細を見る", action: :show, id: item %>
        </nav>
      </td>
      <td><%= link_to item.name, (@cur_node ? {action: :index, cid: item } : node_nodes_path(item)) %></td>
      <td><%= File.basename item.filename %></td>
      <td><%= item.updated.strftime("%Y/%m/%d") %></td>
    </tr>
    <% end %>
  </tbody>
  
</table>
